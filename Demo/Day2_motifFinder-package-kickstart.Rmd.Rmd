```{r echo = FALSE, warning = FALSE, message = FALSE}
knitr::opts_chunk$set(eval = FALSE)
```

- I can start a mitofFinder package

```{r}
available::available("motifFinder")
available::available("motifseeker")
```

- Create the package

```{r}
library(devtools)
create_package('motifSeeker')
```

```{r}
biocthis::use_bioc_description()
usethis::use_mit_license()
usethis::use_readme_md()
```

- Create functions... (`usethis::use_r('scan.R')`)

- Now try it with toy datasets

```{r}
library(GenomicRanges)
ATAC_peaks <- readRDS(here::here('../../../Share/Day1/ATAC_peaks.rds'))
granges <- ATAC_peaks[ATAC_peaks$annot != "non-DA"]
seqlevelsStyle(granges) <- "UCSC"
library(Biostrings)
library(BSgenome.Scerevisiae.UCSC.sacCer3)
seqs <- getSeq(BSgenome.Scerevisiae.UCSC.sacCer3)

x <- scanGRangesForJASPARMotifs(granges, seqs)
```

- Then further document the function 

- Add example

- Add tests (`usethis::use_testthat() ; usethis::use_test('scan.R')`)

